# بازبینی فنی پروژهٔ «آتیران ویزیتور» (Vizitor)

**بستهٔ بررسی‌شده:** `Vizitor-IIS-2.17.1-Final-Package.zip` (۱۲٫۶ مگابایت، ۱۰۸ فایل)
**مخزن:** `Companymeelano/viz` — شاخهٔ `arena/01a0a879-viz` (تنها یک کامیت: «Add files via upload»)
**تاریخ بازبینی:** ۱۶ سپتامبر ۲۰۲۶
**روش:** تحلیل ایستای کد (خواندن کامل لایهٔ داده، شبکه، امنیت، معماری + مرور همهٔ صفحات UI)

> ⚠️ **محدودیت مهم:** در محیط بازبینی نه JDK/Android SDK و نه دسترسی شبکه وجود داشت. علاوه بر آن، خودِ بستهٔ «Final» فایل‌های بیلد (`gradlew`، `settings.gradle.kts`، `build.gradle.kts` ریشه) را ندارد؛ پس **کامپایل واقعی پروژه قابل تأیید نبود**. همهٔ یافته‌های زیر حاصل خواندن کد است و مواردی که به تأیید عملی نیاز دارند، صریحاً علامت‌گذاری شده‌اند.

---

## ۱) خلاصهٔ مدیریتی

پروژه یک اپلیکیشن اندروید **کاملاً فارسی و راست‌چین** برای دامنهٔ پخش آجیل و خشکبار است: کاتالوگ زنده، سبد سفارش، صدور فاکتور با امضای دیجیتال، مشتریان و اعتبارسنجی، گردش حساب، پیگیری چک، اسکنر بارکد، سینک با سرور آتیران (PHP + SQL Server) و لایهٔ آفلاین‌اول با Room.

**نظر صریح من:** به‌عنوان یک محصول، این پروژه در وضعیت **«دموی بسیار خوش‌ساخت ولی هنوز آمادهٔ عملیات واقعی نیست»** است. سمت رابط کاربری و طراحی، سطح بالا و حرفه‌ای است؛ لایهٔ داده و معماری آفلاین‌اول هم اصولی طراحی شده. اما سه دسته مشکل جدی وجود دارد:

1. **بستهٔ تحویلی قابل بیلد نیست** (فایل‌های پایهٔ Gradle غایب‌اند) و **CI/انتشار پر از ناهماهنگی نسخه** است.
2. **چند نقطهٔ پول‌محور اشتباه محاسبه می‌کنند** (مبلغ فاکتور اقلام وزنی) و **دادهٔ جعلی در نسخهٔ release به کاربر واقعی نمایش داده می‌شود** (چک جعلی، پیام جعلی همکاران، واریز جعلی).
3. **بخشی از قابلیت‌های تبلیغ‌شده در عمل وجود ندارند** — نه از طریق UI صدا زده می‌شوند و نه مسیر داده دارند (دستیار هوشمند، کلید همگام‌سازی خودکار، کنترل‌های مدیریتی گفتگو، مسیریابی واقعی، تخفیف/تسویه نقدی).

### کارنامهٔ امتیاز (نظر شخصی، ۰ تا ۱۰)

| حوزه | امتیاز | توضیح کوتاه |
|---|---|---|
| طراحی و UX | **۸٫۰** | سلیقهٔ بصری بالا، تم‌پذیر، فارسیِ تمیز، سازگارسازی سخت‌افزاری (VizitorPerf) |
| معماری لایهٔ داده | **۶٫۵** | آفلاین‌اول اصولی، idempotency، WorkManager — اما بدون DI و بدون تست |
| درستی منطق کسب‌وکار | **۴٫۰** | خطای محاسبهٔ مبلغ فاکتور، دادهٔ جعلی در release، پیام‌های وعدهٔ بی‌پشتوانه |
| امنیت و حریم خصوصی | **۵٫۰** | توکن با Keystore عالی؛ اما admin/admin پیش‌فرض و کلید API بی‌رمز |
| تست و CI/CD | **۲٫۰** | صفر تست، CI با شاخهٔ اشتباه و نشر APK دیباگ با نسخهٔ اشتباه |
| مستندسازی و نگهداشت‌پذیری | **۳٫۰** | کامنت‌های خوب درون‌کد، اما README خالی، بدون لایسنس، پروژه داخل ZIP در گیت |
| آمادگی انتشار (Production) | **۳٫۰** | — |

**جمع‌بندی در یک جمله:** «هستهٔ فنی خوب است، ولی تا محصول شدن حداقل ۲ تا ۳ هفته کار متمرکز روی درستی پول/داده، بیلد و انتشار نیاز دارد.»

---

## ۲) آمار پروژه

| شاخص | مقدار |
|---|---|
| فایل‌های Kotlin | ۴۱ فایل / **۱۱٬۵۹۶ خط** |
| نسبت کامنت | ≈ ۹۶۹ خط (~۸٪) — همه فارسی و توصیفی |
| فایل‌های تست | **۰** (هیچ `src/test` یا `src/androidTest` نداریم) |
| رشته‌های فارسی درون‌کد | **۵۷۳** رشته، در برابر **۲** ورودی در `strings.xml` |
| صفحات بزرگ | `CatalogScreen` ۱۰۳۲ خط، `ChatScreen` ۱۰۳۱، `CustomersScreen` ۹۷۰، `DashboardScreen` ۷۶۲ |
| حجم تصاویر در بسته | ≈ **۱۱٫۹ مگابایت** از ۱۳ مگابایت (فقط `design/` = ۱۰٫۴ مگابایت) |
| دارایی‌های بی‌استفاده | ۶ فایل (~۱۲۰ کیلوبایت): `nut_shopkeeper`، `role_accountant`، `role_manager`، `role_sales_manager`، `role_visitor`، `role_warehouse` |

**پشتهٔ فناوری:** Kotlin + Jetpack Compose (BOM 2024.09.03) + Material3، Room 2.6.1، WorkManager 2.9.1، Retrofit 2.11/OkHttp 4.12/Gson، DataStore، CameraX 1.3.4 + ML Kit Barcode 17.3.0، coroutines 1.8.1، compileSdk/targetSdk 35، minSdk 24، Java 17. **انتخاب نسخه‌ها واقعی، به‌روز و سازگار با هم‌اند** — این خودش نکتهٔ مثبتی است که در پروژه‌های مشابه معمولاً به‌هم می‌ریزد.

---

## ۳) نقاط قوت (چیزهایی که واقعاً خوب انجام شده)

1. **آفلاین‌اول درست پیاده شده است.** خواندن همیشه از Room، نوشتن اول محلی و بعد سینک؛ در `VizitorRepository.syncAll()` صف فاکتورهای `PENDING/FAILED` با ترتیب زمانی ارسال می‌شود و برای هر فاکتور یک `clientInvoiceId` (UUID) تولید و ذخیره می‌شود تا سمت سرور idempotent باشد (`VizitorRepository.kt:245-283` + `AppDatabase.kt:66-73` مهاجرت ۵→۶). این دقیقاً همان کاری است که در اپ‌های میدانی باید انجام شود.
2. **رمزنگاری توکن با Android Keystore واقعاً خوب نوشته شده است** (`AuthStore.kt`): AES-256-GCM، IV تصادفی ذخیره‌شده در ابتدای ciphertext، `GCMParameterSpec(128, iv)`، حالت شکست امن (decrypt ناموفق ⇒ رشتهٔ خالی ⇒ لاگین مجدد). `android:allowBackup="false"` هم درست تنظیم شده.
3. **سازگارسازی خودکار با قدرت دستگاه** (`perf/VizitorPerf.kt`): بر اساس `isLowRamDevice`، RAM، تعداد هسته و نسخهٔ اندروید، سه سطح جلوه (LOW/BALANCED/HIGH) انتخاب می‌شود و جلوه‌های گران‌قیمت (Tilt سه‌بعدی روی آیتم‌های لیست، ذرات، پرتوها) خاموش می‌شوند. این کار نشان می‌دهد تیم به دستگاه‌های ضعیف بازار ایران فکر کرده است.
4. **صفر وابستگی اضافه در جاهای حساس:** فاکتور به PDF/PNG/Word با `PdfDocument` و `Canvas` خود اندروید رندر می‌شود (`data/share/InvoiceShare.kt`)، تبدیل تاریخ جلالی هم دستی پیاده شده (`util/Format.kt`) — برای اپ‌های ایرانی و آفلاین مزیت واقعی است.
5. **سیستم تم چندگانه اصولی است:** ۵ پالت (۳ تیره + ۲ روشن) از یک `CompositionLocal` تغذیه می‌شوند و به Material3 هم تزریق می‌گردند (`ui/theme/Themes.kt`, `Theme.kt`) — به‌جای رنگ‌های پراکنده در کامپوزبل‌ها.
6. **دامنهٔ کسب‌وکار درست مدل شده:** سه سطح قیمت (فروش۱/فروش۲/مصرف‌کننده) با `price2`/`consumerPrice`، واحد «کیلو/کارتن/بسته» در `unit` و `packSize`، اعتبار مشتری (`creditOk`)، درصد افت خرید (`purchaseDropPercent`) برای لیست پیگیری، مانده بدهی و گردش حساب. این‌ها نشان می‌دهد کسی با فروش واقعی آجیل کار کرده است.
7. **محیط فارسی درست:** `LocalLayoutDirection = Rtl` در ریشه (`MainActivity.kt:42`)، فارسی‌سازی ارقام و قیمت (`toFaPrice`, `toFaNumber`, `toFaDigits`)، تجزیهٔ ورودی کاربر با ارقام فارسی/عربی و ممیز «٫» (`parseAmount`) — جزئیاتی که معمولاً فراموش می‌شوند.
8. **اسکنر بارکد مدرن:** CameraX + ML Kit با محدودسازی نرخ پردازش و دوباره‌شماری (`ScannerScreen.kt`)، درخواست مجوز دوربین با `rememberLauncherForActivityResult` (`VizitorNavigation.kt:153,190`).

---

## ۴) یافته‌های بحرانی (P0 — پیش از هر تحویل باید رفع شوند)

### P0-1. بستهٔ «Final» قابل بیلد نیست — فایل‌های پایهٔ Gradle غایب‌اند
`gradlew`, `gradlew.bat`, `settings.gradle.kts`, `build.gradle.kts` (ریشه) و هرگونه version catalog در بسته وجود ندارد؛ فقط `app/build.gradle.kts` و `gradle/wrapper/*` هست.
نتیجه: `./gradlew clean assembleDebug` در `.github/workflows/build-apk.yml:34` قطعاً شکست می‌خورد (فایل اجرایی وجود ندارد)، و نسخهٔ پلاگین‌های `com.android.application`, `org.jetbrains.kotlin.android`, `org.jetbrains.kotlin.plugin.compose`, `com.google.devtools.ksp` هیچ‌جا اعلام نشده است. بنابراین «بستهٔ نهایی» در عمل **یک اسنپ‌شات ناقص از یک وضعیت کاری** است، نه یک بستهٔ قابل تحویل.

### P0-2. محاسبهٔ مبلغ فاکتور در اقلام وزنی اشتباه است (خطای مستقیم مالی)
`VizitorRepository.kt:206`:

```kotlin
val gross = items.sumOf { it.quantity.toLong() * it.unitPrice }
```

`quantity` از نوع `Double` است و برای کالاهای کیلویی (اکثر ۱۲ کالای نمونه) اعشار دارد، اما با `toLong()` **بریده** می‌شود:

- پسته ۱٬۸۵۰٬۰۰۰ ریال × **۲٫۵ کیلو** → سبد به کاربر **۴٬۶۲۵٬۰۰۰** نشان می‌دهد، ولی فاکتوری که ذخیره و به سرور ارسال می‌شود **۳٬۷۰۰٬۰۰۰** است. **(۹۲۵٬۰۰۰ ریال اختلاف — به زیان فروشنده.)**
- بدتر: خط **۰٫۷۵ کیلو** ⇒ `toLong() == 0` ⇒ آن سطر **هیچ** سهمی در جمع فاکتور ندارد، در حالی که `lineTotal` همان سطر ۱٬۳۸۷٬۵۰۰ است. یعنی `grossAmount` فاکتور با جمع `lineTotal` اقلام **نمی‌خواند** — هم برای سرور، هم برای حسابداری.
- ضمناً جمع سبد در UI از SQL می‌آید (`SUM(quantity * unitPrice)` با اعشار درست — `Daos.kt`، `observeTotal()`)، پس «مبلغی که ویزیتور می‌بیند» و «مبلغی که ثبت می‌شود» دو عدد متفاوت‌اند.

اصلاح پیشنهادی: همه‌جا از یک تابع مشترک استفاده شود، مثلاً `fun lineTotal(qty: Double, price: Long) = Math.round(qty * price)` و جمع فاکتور = جمع همان `lineTotal`ها.

### P0-3. دادهٔ ساختگی در نسخهٔ release به کاربر واقعی نشان داده می‌شود
فقط کاشت کاتالوگ/مشتری دمو در `VizitorApp.kt:38` با `BuildConfig.DEBUG` محافظت شده؛ اما **چهار مسیر دیگر بدون هیچ محافظی** دادهٔ جعلی می‌سازند:

| محل | چه چیزی جعل می‌شود |
|---|---|
| `ChequeStore.seedIfEmpty()` (خط ۶۹) + `DashboardScreen.kt:118` | از سه مشتری اول، **چک‌های پیگیری‌شوندهٔ قلابی** با سریال ساختگی (`"7562"+code`)، بانک، مبلغ (۴۸ تا ۸۲ میلیون ریال) و سررسید می‌سازد و در پیشخوان نمایش می‌دهد |
| `VizitorRepository.seedChatIfEmpty()` (خط ۳۴۳) + `VizitorViewModel.kt:172` | **پنج پیام جعلی از همکاران جعلی** («رضا مرادی»، «سارا کاظمی»، «مهدی رستمی») با شمارهٔ موبایل واقعی‌نما در اتاق گفتگو |
| `CustomersScreen.kt:388` | **واریزهای جعلی** (`SeedData.payments`) را به «گردش حساب» مشتری واقعی تزریق می‌کند |
| `DashboardScreen.kt:749` | اگر داده‌ای نباشد، **چارت هفتگی نمایشی** (۳۲ تا ۷۴ میلیون ریال) نشان می‌دهد |

در یک اپ مالی، نمایش چک و واریز جعلی به مامور فروش — که ممکن است بر اساس آن با مشتری تماس بگیرد یا حساب را تسویه‌شده بپندارد — از یک باگ ساده خطرناک‌تر است. همهٔ این‌ها باید یا حذف شوند یا زیر `BuildConfig.DEBUG` بروند.

### P0-4. مقصد «مشتری جدید» بی‌خروجی است (وعدهٔ نادرست در UI)
`addPendingCustomer` (`VizitorRepository.kt:143-180`) مشتری را با **شناسهٔ منفی** و `pendingApproval = true` محلی ذخیره می‌کند و اگر ارسال آنی موفق نشود، پیام UI می‌گوید: «محلی ثبت شد و **در اولین سینک** به آتیران ارسال می‌شود ⏳» (`VizitorViewModel.kt:194`).
اما `syncAll()` هیچ کدی برای **پس‌فرستادن مشتریان در انتظار ندارد** (تنها مصرف‌کنندهٔ `submitCustomer` همان فراخوانی خطیِ خط ۱۶۷ است). همچنین `pendingApproval` هیچ‌جا به `false` برنمی‌گردد. نتیجهٔ عملی:
- مشتریِ ساخته‌شده در حالت آفلاین **هرگز** به سرور نمی‌رسد؛
- برای همیشه از انتخابگر فاکتور حذف است (`CartScreen.kt:193` فیلتر می‌کند) و همیشه برچسب «در انتظار تأیید» دارد؛
- پیام «در اولین سینک ارسال می‌شود» به کاربر **دروغ** است.

### P0-5. کرش تضمینی روی اندروید ۷ (API 24–25) — ترکیب `java.time` با `minSdk 24` بدون Desugaring
`AuthStore.kt:13-15,50` از `java.time.LocalDateTime` / `ZoneOffset` / `DateTimeFormatter` استفاده می‌کند. این APIها از **API 26** در دسترس‌اند و برای `minSdk 24` باید core library desugaring فعال شود:

```kotlin
compileOptions { isCoreLibraryDesugaringEnabled = true }
dependencies { coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.x") }
```

اما در `app/build.gradle.kts` **هیچ اثری از desugaring نیست** (بررسی شد). سناریو: کاربر روی اندروید ۷ لاگین می‌کند (توکن ذخیره می‌شود)، بار بعدی که اپ باز می‌شود `AuthStore.init()` → `isSessionValid()` → `LocalDateTime.parse` → `NoClassDefFoundError` روی **ترد اصلی در `Application.onCreate`** ⇒ **کرش تکراری و غیرقابل‌خروج مگر با پاک‌کردن دادهٔ اپ**. این مورد باید یا با desugaring حل شود، یا با `Calendar`/`SimpleDateFormat` بازنویسی شود، یا `minSdk` به ۲۶ برود.

### P0-6. تنها مسیر اتصال به سرور (HTTPS اجباری) با واقعیت نصب آن‌پرم نمی‌خواند
- `ServerConfig.baseUrl` (`SettingsRepository.kt:34`) پروتکل را **هاردکد `https`** کرده: `"https://$serverIp:$httpPort/..."`.
- منیفست `android:usesCleartextTraffic="false"` دارد، یعنی **HTTP اصلاً امکان‌پذیر نیست**.
- در متن انتشار خودتان (`.github/workflows/build-apk.yml:63+`) سایت IIS روی **پورت ۸۷۳۱** توصیف شده که در نصب‌های داخلی معمولاً HTTP است یا گواهی self-signed دارد.
- هیچ `network_security_config.xml` و هیچ capability برای اعتماد به گواهی داخلی/CA سازمانی وجود ندارد؛ پس یا اتصال برقرار نمی‌شود، یا دست‌کم در گواهی self-signed به `SSLHandshakeException` می‌خورد.
- ضمناً پیش‌نمایش زندهٔ تنظیمات آدرس را با `.../api.php` نشان می‌دهد (`SettingsScreen.kt:226-233`)، ولی همهٔ فراخوانی‌ها به `index.php?action=...` می‌روند (`ApiService.kt`). اسکریپت‌های سرور (`api.php`/`index.php`, `Setup-VizitorServer.ps1`, `config.php`) **در این بسته نیستند**، پس قرارداد سرور هم از این مخزن قابل راستی‌آزمایی نیست.

### P0-7. کلید API و اعتبارنامه‌های مدیریتی محافظت نشده‌اند
- `ServerConfig.apiKey` در DataStore **بدون رمزنگاری** ذخیره می‌شود (`SettingsRepository.kt:44,57`) و در هر درخواست به‌عنوان هدر `X-Api-Key` می‌رود.
- `ChatPrefs.kt:56-58`: در اولین اجرا خط مدیریت با **`admin` / `admin`** ساخته می‌شود و `verifyAdmin` (خط ۱۰۰-۱۱۱) رمز را با **SHA-256 بدون salt** نگه می‌دارد.
- مهم‌تر: اتاق گفتگو **اصلاً بخش سروری ندارد** — «گروه‌ها»، «قفل گروه»، «مسدودسازی اعضا»، «مدیر» همه فقط در SharedPreferences همان دستگاه‌اند. یعنی تمام کنترل‌های مدیریتی در واقع **امنیت نمایشی (security theater)** هستند: نه چیزی را کنترل می‌کنند و نه منتقل می‌شوند.

### P0-8. نسخه‌ها و انتشار به‌کل ناهماهنگ‌اند
| منبع | مقدار |
|---|---|
| `app/build.gradle.kts:24-25` | `versionCode = 30`, `versionName = "2.14.1"` |
| نام بستهٔ ZIP | **2.17.1** |
| `.github/workflows/build-apk.yml` | تگ/فایل/عنوان = **v2.13.4**، خروجی `app-debug.apk` |

و این‌که CI روی `assembleDebug` است، یعنی «نسخهٔ قابل نصب» منتشرشده در ریلیز:
- **دیباگ** است (`debuggable = true`)؛
- `applicationIdSuffix = ".debug"` دارد ⇒ به‌صورت `ir.atiran.vizitor.debug` نصب می‌شود، **جدا از اپ رسمی** — یعنی روی گوشی‌هایی که نسخهٔ قبلی نصب است، به‌روزرسانی «ارتقایی» رخ نمی‌دهد و دو اپ کنار هم می‌نشینند؛
- `versionNameSuffix = "-debug"` دارد؛
- هیچ `signingConfig` برای release تعریف نشده ⇒ `assembleRelease` خروجی امضاشدهٔ قابل انتشار تولید نمی‌کند.

همچنین در متن ریلیز منتشرشده، **placeholder جایگزین‌نشده** `SERVER-CONFIGURED-BY-INSTALLER` باقی مانده است.

---

## ۵) یافته‌های مهم (P1)

### P1-1. قابلیت‌های تبلیغ‌شده‌ای که در عمل هیچ‌جا صدا زده نمی‌شوند
| قابلیت | شاهد |
|---|---|
| **دستیار هوشمند فروش (Gemini)** | `askAiAssistant`/`aiSuggestion`/`aiLoading` در `VizitorViewModel.kt:120-124,238-259` تعریف شده‌اند اما **هیچ صفحه‌ای آن‌ها را مصرف نمی‌کند** (جست‌وجوی کل درخت کد: صفر مصرف‌کننده در UI). حتی اگر صدا زده شود، ورودی‌اش `repo.salMaliFor()` است — یعنی فقط کش محلی — و `fetchSalMaliFor()` که همان جدول `sal_mali` را از سرور می‌گیرد (`VizitorRepository.kt:318`) **مرده است**؛ روی نسخهٔ release که کاشت داده هم غیرفعال است، تاریخچهٔ خرید همیشه خالی است. |
| **تخفیف و تسویهٔ نقدی** | `computeDiscount()` نوشته شده و در VM هم اکسپوز شده، اما `issueInvoice` خودش `discount = 0L` می‌گذارد (`VizitorRepository.kt:208`) و پارامتر `cashSettlement` **اصلاً استفاده نمی‌شود**؛ UI هم همیشه `false` می‌فرستد (`CartScreen.kt:315`). |
| **کلید «همگام‌سازی خودکار»** | `SettingsScreen.kt:314` مقدار را ذخیره می‌کند، ولی هیچ‌جای کد `autoSync` خوانده نمی‌شود (بررسی شد). `SyncWorker.schedule()` در `VizitorApp.kt:40` هم بی‌قیدوشرط هر ۱۵ دقیقه اجرا می‌شود. ⇒ سوئیچ، تزئینی است. |
| **اطلاع‌رسانی «فاکتور شما ثبت شد»** | `SyncWorker.notifyUser` هر ۱۵ دقیقه نوتیفیکیشن می‌زند، اما مجوز `POST_NOTIFICATIONS` (که در منیفست اعلام شده) **هیچ‌گاه در زمان اجرا درخواست نمی‌شود** (تنها مجوز درخواستی در کل اپ `CAMERA` است). روی اندروید ۱۳+ هیچ نوتیفیکیشنی نمایش داده نمی‌شود ⇒ از یک طرف قابلیت شاخص کار نمی‌کند، از طرف دیگر اگر بعداً درست شود، **هر ۱۵ دقیقه اسپم** می‌شود. |
| **«بهینه‌سازی مسیر ویزیت»** | در واقع فقط `sortedBy` فاصلهٔ خط‌راست از یک **موقعیت هاردکد تهران** (`CustomersScreen.kt:139`) است — بدون موقعیت واقعی، بدون درنظرگرفتن ترافیک/جاده و بدون الگوریتم مسیریابی (TSP). نقشهٔ `MiniRouteMap` هم یک Canvas شماتیک است، نه نقشهٔ واقعی. |

### P1-2. فرسایش نشست و نبود مسیر بازیابی (auth)
- `AuthStore.kt:50-51` زمان انقضای سرور را با `ZoneOffset.UTC` تفسیر می‌کند؛ اگر سرور (مثل سرور ایران) **زمان محلی** بفرستد، این مقدار ۳٫۵ ساعت زودتر از واقع منقضی می‌شود ⇒ لاگین‌های زودهنگام و سردرگمی کاربر.
- هیچ interceptor احراز هویتی، هیچ مدیریت ۴۰۱ و هیچ refresh/token-renew وجود ندارد. با انقضای توکن، همهٔ سینک‌ها با متن فارسی «ورود به سرور انجام نشده است» شکست می‌خورند و کاربر هیچ راهنمایی برای لاگین مجدد نمی‌بیند (صفحهٔ Login فقط در استارت‌اپ و بار اول نشان داده می‌شود).
- `HttpLoggingInterceptor` با سطح `BASIC` در **build خود release** فعال است (`RetrofitClient.kt:21-23`) ⇒ آدرس‌ها و پارامترهای درخواست در logcat باقی می‌مانند. بهتر است سطح لاگ بر اساس `BuildConfig.DEBUG` تنظیم شود.

### P1-3. پیکربندی پراکسی هوش مصنوعی ناتمام و ناایمن
- مدل پیش‌فرض `gemini-1.5-flash` است (`SettingsRepository.kt:30`) — خانوادهٔ Gemini 1.5 **بازنشسته** شده و درخواست‌های آن اکنون ۴۰۴ برمی‌گردانند؛ باید به مدل فعال (مثل 2.5-flash/2.5-flash-lite) منتقل شود.
- آدرس Worker پیش‌فرض، placeholder است: `https://vizitor-ai-proxy.YOUR-SUBDOMAIN.workers.dev`.
- اپ **هیچ سندی/کلید مشترکی** به Worker نمی‌فرستد (`ai/GeminiAssistant.kt`)، پس هر کسی که آدرس Worker را بداند می‌تواند سهمیهٔ Gemini شما را مصرف کند. حداقل یک هدر راز مشترک یا امضای HMAC لازم است.
- و از آنجا که کل درخواست‌ها از دستگاه کاربر می‌آید، `workerUrl` قابل تغییر توسط کاربر است ⇒ در نسخه‌های بعدی بهتر است این تنظیم از UI حذف و در `BuildConfig` هاردکد شود.

### P1-4. لایهٔ داده: سخت‌سازی نشده
- `AppDatabase.kt:27` → `exportSchema = false` ⇒ هیچ تاریخچهٔ اسکیمایی برای راستی‌آزمایی مهاجرت‌ها وجود ندارد (و اصلاً فایل schema در بسته نیست).
- مهاجرت‌ها از نسخهٔ ۳ شروع می‌شوند. کاربری که روی نسخهٔ ۱ یا ۲ باشد ⇒ `IllegalStateException` در باز کردن دیتابیس، بدون `fallbackToDestructiveMigration` و بدون مسیر تشخیص.
- دیتابیس **رمزنگاری نشده** است در حالی که درونش بدهی مشتری، امضای دیجیتال (`signatureBase64`) و تاریخچهٔ فاکتور است. رمزنگاری توکن با Keystore عالی است ولی با مدل تهدید ناهمگون است؛ استفاده از SQLCipher یا حداقل مستندسازی آگاهانه لازم است.
- هیچ محدودیت اندازه‌ای برای `chat_messages` و `sal_mali_history` نیست و هیچ پاک‌سازی دوره‌ای وجود ندارد.

### P1-5. قواعد ProGuard عملاً بی‌اثرند
`app/proguard-rules.pro:13-14` بستهٔ `ir.atiran.vizitor.data.remote.dto.**` را keep می‌کند، اما چنین بسته‌ای **وجود ندارد**؛ DTOها در `ir.atiran.vizitor.data.remote` هستند. فعلاً به لطف `-keepattributes *Annotation*` و `@SerializedName` روی همهٔ فیلدها JSON سالم می‌ماند، ولی این قاعدهٔ اشتباه توهم ایمنی می‌سازد و برای Room/Gson/WorkManager هم قاعدهٔ صریحی وجود ندارد.

### P1-6. فقدان کامل تست و کیفیت‌سنجی
- صفر تست واحد/ابزاره. برای منطق مالی (`issueInvoice`, `computeDiscount`, `syncAll`, تبدیل تاریخ جلالی، `parseAmount`، مهاجرت‌های Room) تست واحد **کم‌هزینه و پرمنفعت** است.
- هیچ lint/detekt/ktlint در CI نیست؛ هیچ workflow برای pull request یا بررسی امنیتی وجود ندارد.
- تنها workflow موجود، APK را می‌سازد و در صورت شکست، **`build.log` را داخل مخزن کامیت و پوش می‌کند** (`.github/workflows/build-apk.yml:36-44`) — کار درستی نیست؛ خروجی لاگ باید `actions/upload-artifact` شود.

### P1-7. CI با این مخزن جور نیست
- تریگر: `branches: [arena/01a09f68-vizitor]` — چنین شاخه‌ای در این مخزن **وجود ندارد**، پس workflow با push هرگز اجرا نمی‌شود (فقط دستی).
- گام خطا، به مخزن دیگری push می‌کند: `.../Companymeelano/Vizitor.git` در حالی که این checkout `Companymeelano/viz` است؛ با `GITHUB_TOKEN` این مخزن، چنین push معمولاً رد می‌شود.
- `action-gh-release` با تگ ثابت `v2.13.4` ⇒ هر بار اجرا، همان ریلیز/تگ را بازنویسی می‌کند.

---

## ۶) یافته‌های کیفی و جزئی (P2)

1. **نسخه‌های تکراری در ViewModel:** دو بلوک `init { }` جداگانه (`VizitorViewModel.kt:94,171`) — قانونی ولی گیج‌کننده.
2. **تکرار کد در صفحه‌ها:** `SearchField`, `CustomerSearchField`, `RegField`, کارت‌های آماری در چند صفحه کپی شده‌اند؛ لیست importهای `ReportsScreen` کپیِ سوئیچ تم/OCRک/تنظیمات است که هرگز استفاده نمی‌شود (نشانهٔ کپی‌پیست بین دو صفحه با اندازهٔ ۳۰۰ و ۶۰۰ خط).
3. **باقی‌ماندهٔ دامنهٔ اشتباه:** متن پیشنهاد جایگزین دستیار هوشمند از **دنیای خودرو** آمده: «فیلتر روغن و فیلتر هوا مکمل هر خرید **روغن موتور** هستند 🧰» (`VizitorViewModel.kt:254`) — در اپ آجیل و خشکبار قطعاً ناخواسته است.
4. **تارگت فروش روزانه هاردکد:** `dailyTarget = 250_000_000L` با کامنت «در حالت واقعی از سرور می‌آید» (`VizitorRepository.kt:74`).
5. **کد مرده:** `computeDiscount`، `ChequeStore` فقط محلی، `SeedData` در release بی‌مصرف، بخش قابل توجهی از نقش‌های صفحهٔ SplashScreen (۵ از ۶ نقش «به‌زودی»).
6. **بدون DI:** `VizitorRepository` در `VizitorApp:36`, `VizitorViewModel:31`, `SyncWorker:36` مستقیم ساخته می‌شود و چند «چیز-جهانیِ حالت‌دار» (`VizitorPerf`, `ThemeManager`, `ChatPrefs`, `ChequeStore`) داریم ⇒ تست‌پذیری پایین.
7. **همه‌چیز در یک ماژول:** یک ماژول `:app` با ۱۱٬۶۰۰ خط و صفحات ۱۰۰۰ خطی. حداقل جدا کردن `:core:data`, `:core:ui`, `:feature:*` به نگهداشت‌پذیری کمک جدی می‌کند.
8. **۱۵ `rememberInfiniteTransition` و یک حلقهٔ `while(true)` در `Tilt3D.kt:57`** برای هر آیتم لیست. `VizitorPerf` تا حد زیادی مهارش می‌کند، ولی روی دستگاه High (پیش‌فرض برای اکثر گوشی‌های ۶ هسته‌ای به بالا) ریسک پرش فریم و مصرف باتری در لیست‌های بلند وجود دارد. پیشنهاد: افکت‌ها فقط برای آیتم‌های visible و با `graphicsLayer` (نه `composed` برای هر آیتم).
9. **بومی‌سازی:** ۵۷۳ رشتهٔ فارسی سخت‌کدشده در برابر ۲ ورودی `strings.xml`. حتی اگر امروز فقط فارسی لازم است، انتقال به منابع، شرط لازم هرگونه تغییر زبان در آینده و رعایت معمول اندروید است.
10. **دسترس‌پذیری:** برچسب تب‌ها با `fontSize = 9.5.sp` (کوچک‌تر از حد توصیه‌شده)، وضعیت اعتبار فقط با **رنگ** (سبز/قرمز) منتقل می‌شود (برای کوررنگی نامناسب)، و `usesCleartextTraffic`/تنظیمات دیگر رسمیِ خوبی مثل `contentDescription` (۶۹ مورد) تا حدی رعایت شده — یعنی مسیر بهبود کوتاه است.
11. **مدیریت چرخه لایف‌سایکل اپ:** `installSplashScreen()`، `enableEdgeToEdge()` و کنترل رنگ نوار وضعیت خوب است؛ اما هیچ حلقهٔ بازیابی برای «دیتابیس خراب / فضای پر» وجود ندارد.
12. **نامهربانی‌های محصولی:** دکمهٔ «بازگشت به پیش‌فرض امن HTTPS (api.vizitor.local:443)» آدرسی می‌دهد که در دنیای واقعی resolve نمی‌شود (`api.vizitor.local` یک نام مدل‌سازی‌شده است)؛ مقدار پیش‌فرض `apiKey` هم خالی است ⇒ برای مشتری تازه، اولین تجربه یک بن‌بست اعتبارسنجی است.

---

## ۷) Hygiene مخزن (به همان اندازهٔ کد مهم)

- کل پروژه به‌صورت یک **ZIP ۱۲٫۶ مگابایتی درون گیت** ذخیره شده، با یک کامیت «Add files via upload». پیامدها: هیچ diff، هیچ بازبینی‌ای خط‌به‌خط (code review)، هیچ blame، و هیچ راهی برای حل تعارض. Git نمی‌تواند ZIP را دلتا‌کد کند ⇒ تاریخچهٔ مخزن برای همیشه با نسخه‌های تکراری ۱۲ مگابایتی باد می‌کند.
- `README.md` فقط `# viz` است: نه معرفی، نه راهنمای راه‌اندازی سرور/اپ، نه معماری، نه اسکرین‌شات (با اینکه تصاویر طراحی وجود دارد)، نه `LICENSE`، نه `CONTRIBUTING`، نه سیاست نسخه‌گذاری.
- ۱۰ مگابایت تصویر در `design/` (آیکون‌های ۱٫۵ تا ۲٫۲ مگابایتی) — جای این‌ها Git LFS یا یک فضای بیرونی است.
- اسکریپت‌های سرور (PHP/SQL/PowerShell) که متن ریلیز به آن‌ها ارجاع می‌دهد، **در مخزن نیستند** ⇒ محصول از این مخزن قابل بازتولید نیست و قرارداد کلاینت↔سرور مستند نشده است (فقط از روی `ApiService.kt` و DTOها قابل حدس است).
- فایل‌های داخل ZIP تاریخ امروز (۲۰۲۶-۰۹-۱۶) را دارند و شاخهٔ تریگر CI شبیه یک شاخهٔ موقت است ⇒ این بسته ظرفاً یک **اسنپ‌شات کارِ نیمه‌تمام** است.

---

## ۸) نقشهٔ راه پیشنهادی (به ترتیب اولویت و هزینه)

### گام ۰ — یک روز: قابل‌بیلد و قابل‌بازبینی کردن
1. پروژه را از ZIP بیرون بیاورید و **ساختار پوشه‌ای واقعی** را کامیت کنید (سورس + منابع؛ تصاویر طراحی به LFS یا خارج از مخزن).
2. `gradlew`, `gradlew.bat`, `settings.gradle.kts`, `build.gradle.kts` ریشه و `gradle/libs.versions.toml` را اضافه کنید و یک‌بار `./gradlew assembleDebug` را **سبز** کنید.
3. `.gitignore` (موجود) + `README` واقعی بنویسید: معرفی، پیش‌نیاز، نحوهٔ ساخت سرور، نحوهٔ تزریق `apiKey`/آدرس سرور، لایسنس.

### گام ۱ — ۲ تا ۳ روز: درستی پول و داده
4. `gross` فاکتور را با گرد کردن‌درست حساب کنید (P0-2) و **تست واحد** برای اقلام ۰٫۲۵/۰٫۵/۲٫۵ کیلو بنویسید.
5. تمام مسیرهای دادهٔ نمایشی را حذف یا زیر `BuildConfig.DEBUG` ببرید (P0-3: `ChequeStore.seedIfEmpty`, `seedChatIfEmpty`, `SeedData.payments`, `buildWeekly`).
6. برای مشتریان در انتظار، صف واقعی سینک بسازید (P0-4) و `pendingApproval` را پس از تأیید سرور به `false` برگردانید.
7. desugaring را فعال کنید یا `minSdk` را به ۲۶ ببرید (P0-5) — این مورد یک کرش قطعی است.

### گام ۲ — یک هفته: اتصال و امنیت
8. مدل آدرس سرور را به «scheme + host + port» (یا ورودی URL کامل) ارتقا دهید و برای شبکهٔ داخلی `network_security_config` با trust-anchor سفارشی بگذارید (P0-6).
9. کلید API را به Keystore/EncryptedSharedPreferences منتقل کنید؛ رمز پیش‌فرض `admin/admin` را حذف و رمز اولیه را در نصب ست کنید؛ سند مشترک برای Worker تعریف کنید (P0-7, P1-3).
10. interceptor احراز هویت + مدیریت ۴۰۱ + لاگ فقط در DEBUG + درخواست مجوز `POST_NOTIFICATIONS` و شرطی‌کردن نوتیفیکیشن‌ها (P1-2, P1-1).

### گام ۳ — یک هفته: انتشار حرفه‌ای
11. نسخه‌گذاری واحد: یک منبع حقیقت برای `versionCode/versionName`، تگ و نام فایل APK (P0-8).
12. `signingConfig` برای release + ساخت `assembleRelease` + انتشار همان APK امضاشده (نه دیباگ) و حذف `applicationIdSuffix` از مسیر انتشار عمومی.
13. CI را برای این مخزن بازنویسی کنید: تریگر روی شاخه‌های واقعی/PR، اجرای `test`/`lint`، آپلود آرتیفکت، ممنوعیت push کردن لاگ در مخزن (P1-6, P1-7).

### گام ۴ — پیوسته: کیفیت
14. تفکیک ماژول‌ها، حذف کد مرده (دستیار هوشمند یا وصل شود یا حذف، تخفیف/تسویه یا پیاده یا پاک)، انتقال رشته‌ها به `strings.xml`، افزودن detekt/ktlint، و نوشتن تست برای مهاجرت‌های Room و منطق گزارش‌ها.

---

## ۹) پیوست: فهرست سریع شواهد (برای پیگیری)

| موضوع | فایل:خط |
|---|---|
| خطای جمع فاکتور | `data/repository/VizitorRepository.kt:206` |
| تخفیف صفر و پارامتر بی‌استفاده | `data/repository/VizitorRepository.kt:190-208`، `VizitorViewModel.kt:217-222`، `ui/screens/CartScreen.kt:315` |
| چک جعلی | `data/local/ChequeStore.kt:69-86`، `ui/screens/DashboardScreen.kt:115-118` |
| پیام گفتگوی جعلی | `data/repository/VizitorRepository.kt:343-358`، `VizitorViewModel.kt:171-173` |
| واریز جعلی | `ui/screens/CustomersScreen.kt:388-391` |
| چارت جعلی | `ui/screens/DashboardScreen.kt:749` |
| موقعیت هاردکد + مسیریابی نمایشی | `ui/screens/CustomersScreen.kt:139,146`، `ui/components/MiniRouteMap.kt` |
| توکن/نشست | `data/local/AuthStore.kt:13-15,49-52` |
| کلید API بی‌رمز | `data/repository/SettingsRepository.kt:34,44,57` |
| admin/admin | `data/local/ChatPrefs.kt:56-58,100-111` |
| آدرس/مدل AI | `data/repository/SettingsRepository.kt:29-30`، `ai/GeminiAssistant.kt:52-56` |
| سوئیچ بی‌اثر همگام‌سازی | `ui/screens/SettingsScreen.kt:313-317`، `VizitorApp.kt:40` |
| نوتیفیکیشن بدون مجوز | `data/sync/SyncWorker.kt:50-66`، `AndroidManifest.xml:17` |
| دیتابیس بدون schema اکسپورت | `data/local/AppDatabase.kt:26-27,81` |
| قاعدهٔ اشتباه ProGuard | `app/proguard-rules.pro:13-14` |
| نسخه‌ها | `app/build.gradle.kts:24-25`، `.github/workflows/build-apk.yml:10,50,62-63` |

---

### یک جملهٔ پایانی
این پروژه از نظر «حس محصول» و «فهم دامنه» بالاتر از میانگین است و پایهٔ فنی خوبی دارد؛ اما تا وقتی **مبلغ فاکتور در اقلام کیلویی** درست نشود و **دادهٔ جعلی از نسخهٔ release بیرون نیاید**، نمی‌توان آن را به یک پخش واقعی داد — چون در آن صورت، خطای اول به زیان مالی و دومی به بی‌اعتمادی عملیاتی تبدیل می‌شود.
