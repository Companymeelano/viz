<?php
/* ═══════════════════════════════════════════════════════════════════════════
   Vizitor — آتیران ویزیتور | فایل تنظیمات وب‌سرویس (نمونه)
   Developed by Milano Technical Team, Milad Yaghoobi
   ─────────────────────────────────────────────────────────────────────────
   این فایل «نمونه» است. اسکریپت Setup-VizitorServer.ps1 به‌طور خودکار آن را
   با مقادیر واقعی به config.php تبدیل می‌کند (کلید API تصادفی، رمز دیتابیس و…).

   نصب دستی:  همین فایل را به config.php کپی کنید و مقادیر را پر کنید.
   امنیت:     config.php هرگز نباید در مخزن گیت یا فضای عمومی قرار بگیرد.
   ═══════════════════════════════════════════════════════════════════════════ */
return [

    /* ── اتصال به SQL Server (دیتابیس Vizitor) ─────────────────────────── */
    'db' => [
        // نام نمونه یا «.\INSTANCE» یا «SERVER\INSTANCE» یا IP سرور دیتابیس
        'host'                  => 'localhost',
        'port'                  => 1433,
        // نام دیتابیس ساخته‌شده توسط نصب‌کننده (پیش‌فرض AtiranVizitor)
        'name'                  => 'AtiranVizitor',
        // کاربر SQL با دسترسی فقط به همین دیتابیس (اصل کمترین دسترسی)
        'user'                  => 'vizitor_app',
        'pass'                  => 'CHANGE_ME',
        // برای SQL Server روی همین سرور معمولاً رمزنگاری لازم نیست
        'encrypt'               => false,
        'trust_server_certificate' => true,
    ],

    /* ── کلید API مشترک با اپ اندروید ───────────────────────────────────── */
    // همین مقدار را در اپ → تنظیمات → «کلید API» وارد کنید.
    'api_key'   => 'CHANGE_ME',

    /* ── شناسهٔ نسخه وب‌سرویس (در کارت سلامت اپ نمایش داده می‌شود) ─────── */
    'api_version' => '1.4.0',

    /* ── زمان و نشست ─────────────────────────────────────────────────────── */
    'timezone'          => 'Asia/Tehran',   // فقط برای لاگ‌ها؛ زمان API همیشه UTC است
    'token_ttl_minutes' => 720,             // ۱۲ ساعت اعتبار توکن ورود

    /* ── ایمنی ورود ──────────────────────────────────────────────────────── */
    'max_login_attempts'    => 5,   // پس از این تعداد خطا، ورود موقتاً بسته می‌شود
    'login_lockout_minutes' => 15,

    /* ── رفتار کسب‌وکار ─────────────────────────────────────────────────── */
    // بررسی درستی جمع فاکتور (جمع اقلام = جمع ناخالص). توصیه: true
    'strict_totals'     => true,
    // کسر موجودی سمت سرور هنگام ثبت فاکتور. توصیه: false
    // (موجودی مرجع، موجودی نرم‌افزار حسابداری است؛ در حالت نماهای ERP این گزینه بی‌اثر است)
    'decrement_stock'   => false,
    // درج خودکار فاکتور در جداول حسابداری واقعی (در حالت نماهای ERP)
    // false = ذخیره در VizitorSalesHeader/Lines برای بازبینی حسابداری
    'write_to_erp'      => false,

    /* ── لاگ و اشکال‌زدایی ──────────────────────────────────────────────── */
    'log_requests'      => true,   // ثبت درخواست‌ها در جدول VizitorApiLog
    'allow_cors_debug'  => false,  // true فقط برای تست با مرورگر/Postman
];
